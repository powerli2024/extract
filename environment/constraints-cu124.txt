# 统一环境的关键约束。PyTorch 三件套由 setup_env.sh 从官方 cu124 索引先安装。
# 这里再次约束，防止 ClearVoice / Qwen-ASR 的传递依赖把 CUDA 栈升级。
torch==2.6.0
torchaudio==2.6.0
torchvision==0.21.0

# ClearVoice 0.1.2 的官方硬约束。
numpy==1.26.4
librosa==0.10.2.post1
soundfile==0.12.1
opencv-python==4.10.0.84
rotary-embedding-torch==0.8.3

# Qwen-ASR 0.0.6 的官方硬约束。
qwen-asr==0.0.6
transformers==4.57.6
accelerate==1.12.0
nagisa==0.2.11
soynlp==0.0.493

# ONNX Runtime 1.20.x 是 CUDA 12.x + cuDNN 9.x 组合。
onnxruntime-gpu==1.20.2
